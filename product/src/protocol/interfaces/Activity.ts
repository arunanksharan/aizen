interface SocialActivityInterface {
  // Social activity metrics
  followerCount?: number;
  followingCount?: number;
  postCount?: number;
  likeCount?: number; // Total likes received across all posts
  sharedCount?: number; // Total shares across all posts
  commentCount?: number; // Total comments across all posts
}
