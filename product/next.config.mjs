/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'oaidalleapiprodscus.blob.core.windows.net',
      'cdn.openart.ai',
      'oaidalleapiprodscus.blob.core.windows.net',
    ],
  },
};

export default nextConfig;
